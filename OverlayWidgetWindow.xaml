<Window x:Class="NakTaWallpaper.OverlayWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Native PoC Controls"
        Width="320"
        Height="160"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="False"
        Background="Transparent"
        AllowsTransparency="True">
    <Border Background="#FCF8FF"
            BorderBrush="#D9C6F3"
            BorderThickness="1"
            CornerRadius="16"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Native WPF Click Test"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#352C42" />

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Button Content="Settings"
                        Width="112"
                        Height="36"
                        Margin="0,0,10,0"
                        Click="SettingsButton_Click" />
                <Button Content="Refresh"
                        Width="112"
                        Height="36"
                        Click="RefreshButton_Click" />
            </StackPanel>

            <TextBlock x:Name="MemoryTextBlock"
                       Grid.Row="2"
                       FontSize="12"
                       Foreground="#716381"
                       TextTrimming="CharacterEllipsis" />
        </Grid>
    </Border>
</Window>
